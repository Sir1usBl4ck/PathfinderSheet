<UserControl x:Class="UserInterface.Views.MainTabViews.CharacterTabViews.CharacterSkills.SkillListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views.MainTabViews.CharacterTabViews.CharacterSkills"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="1">
        <Button  Command="{Binding DataContext.RollCommand, RelativeSource={RelativeSource FindAncestor, AncestorLevel=2, AncestorType=UserControl}}"
                CommandParameter="{Binding}">
            <Button.Content>
                <WrapPanel>
                    <TextBlock Text="{Binding BonusToRoll, FallbackValue=10}" Margin="0,0,20,0"/>
                    <TextBlock Text="{Binding Name, FallbackValue=Knw.(Dungeoneering)}" Width="120" FontSize="16"/>
                </WrapPanel>
            </Button.Content>
        </Button>
    </Border>
</UserControl>
