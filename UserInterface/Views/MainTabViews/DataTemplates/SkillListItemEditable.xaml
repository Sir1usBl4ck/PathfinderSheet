<UserControl x:Class="UserInterface.Views.MainTabViews.DataTemplates.SkillListItemEditable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views.MainTabViews.DataTemplates"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="1">
        <WrapPanel>
            <TextBox Text="{Binding Rank, FallbackValue=10, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0" Width="40"/>
            <TextBlock Text="{Binding Name, FallbackValue=Knw.(Dungeoneering)}" Width="120" FontSize="16"/>
        </WrapPanel>
    </Border>
</UserControl>
