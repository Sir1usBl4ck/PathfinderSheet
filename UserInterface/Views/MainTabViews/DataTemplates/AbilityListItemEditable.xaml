<UserControl x:Class="UserInterface.Views.MainTabViews.DataTemplates.AbilityListItemEditable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views.MainTabViews.DataTemplates"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <TextBox Margin="2,0,5,0">
                <TextBox.Text>
                   <Binding Path="BaseScore" FallbackValue="99" />
                </TextBox.Text>
            </TextBox>
            <TextBlock Text="{Binding Name, FallbackValue=Broken}" FontSize="14"/>
           
            
        </DockPanel>

    </Grid>
</UserControl>
