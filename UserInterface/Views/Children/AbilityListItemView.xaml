<UserControl x:Class="UserInterface.Views.AbilityListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views"
             mc:Ignorable="d" MinHeight="30" d:DesignWidth="800">

    <Border Margin="0,0,0,5">
        <DockPanel LastChildFill="False">
            <TextBlock Text="{Binding Name}" TextAlignment="Left" VerticalAlignment="Center" ToolTip="This is the abilities name"
                       FontWeight="Bold" FontSize="13" MinWidth="100" />
            <ComboBox ItemsSource="{Binding BaseValues}" SelectedItem="{Binding BaseScore, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

            <TextBlock Text="{Binding Score}" Margin="5,0,0,0" />

            <TextBlock Text="{Binding Modifier}" Margin="5,0,0,0" />
        </DockPanel>
    </Border>
</UserControl>
