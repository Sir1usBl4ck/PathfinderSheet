<UserControl x:Class="UserInterface.Views.SaveListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views"
             mc:Ignorable="d" 
             d:MinHeight="30" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Name}" TextAlignment="Center" Margin="5,0"  VerticalAlignment="Center" ToolTip="{Binding Ability.Name}"
                   FontWeight="Bold" FontSize="13" MinWidth="50" />

        <TextBlock Text="{Binding Bonus}" Margin="5,0" VerticalAlignment="Center" MinWidth="50" TextAlignment="Center"/>

    </StackPanel>
</UserControl>
